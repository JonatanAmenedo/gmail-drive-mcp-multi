# Dependencies
node_modules/

# Build output
dist/

# Credentials (never commit these!)
credentials.json
oauth-keys.json
*.credentials.json

# OS files
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log
npm-debug.log*

# Test
coverage/

# Environment
.env
.env.local

# Claude Code session metadata (worktrees, transcripts)
.claude/

# Python virtual environments (not used here; guard against accidental commits)
venv/
.venv/
*.venv/

# Local backups of dist (e.g. created during deployment to Cowork)
dist.bak*

# Local config home (should never be inside the repo, but guard anyway)
.gmail-mcp/

# Local, machine-specific account map for the gmail-accounts skill (contains real addresses)
skills/*/accounts.local.md
